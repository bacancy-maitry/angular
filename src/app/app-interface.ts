export interface AppInterface {
    userId: number,
    id: number,
    // name: string,
    // year: number,
    // color: string,
    // pantone_value: number,
    title: string,
    body: string,
}
